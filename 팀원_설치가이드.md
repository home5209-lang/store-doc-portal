# 발신번호 등록 관리자 — 팀원 설치 가이드

**Node 설치 같은 거 필요 없어요.** 압축만 풀고 `.env` 파일 하나 넣으면 바로 씁니다.
(웹 주소로 접속하는 게 아니라, 내 PC에서 프로그램을 켜고 브라우저로 여는 방식이에요.)

---

## 처음 한 번만 (준비)

### 1. 프로그램 압축 풀기
- 관리자(김시온)에게 받은 **`sender-admin-dist-날짜.zip`** 을 **원하는 위치에 압축 풀기**.
  - 예: `바탕화면\sender-admin`
- 안에 이미 실행에 필요한 것(프로그램 + 자동화용 브라우저)이 다 들어 있어요.

### 2. 설정 파일(.env) 넣기
- 관리자에게 **`.env` 파일**을 **따로** 받아, 압축 푼 폴더 안(**`START.bat` 과 같은 위치**)에 그대로 넣으세요.
- ⚠️ `.env` 에는 비밀값이 들어 있으니 **외부에 공유하지 마세요.** (메신저·메일에 올리지 않기)
- `.env` 없이 `START.bat` 을 누르면 *".env 파일을 넣으라"* 는 안내가 뜨고 종료됩니다.

> Node.js 설치, `설치.bat` 실행 같은 과정은 **없습니다.** 이 번들에 다 포함돼 있어요.

---

## 매일 사용하기

### A. 프로그램 켜기
- **`START.bat`** 더블클릭 → 검은 창이 뜨고 서버가 켜집니다. **이 창은 켜 두세요.** (닫으면 꺼져요)
- 잠시 후 브라우저가 자동으로 열립니다. 안 열리면 직접 **http://localhost:3000** 접속.
- **본인 회사 구글 계정**으로 로그인 → 사용 시작.

### B. NHN 로그인 (발신번호 등록 신청할 때만)
- 발신번호 **등록 신청(제출)** 을 하려면 NHN 로그인이 필요합니다.
- **`NHN-LOGIN.bat`** 더블클릭 → 브라우저가 열리면 **본인 NHN 계정**으로 로그인 → 안내대로 Enter.
- 한 번 로그인하면 세션이 유지되고, 대시보드 상단 배너에 **"NHN 로그인됨"** 으로 표시됩니다.
  - 배너가 **"NHN에 로그인되어 있지 않습니다"** 로 바뀌면(세션 만료) → `NHN-LOGIN.bat` 을 다시 실행하세요.

---

## 사용 흐름 요약

1. `START.bat` → 브라우저에서 구글 로그인
2. 매장 검색 → **링크 발급** → 매장에 전달 (링크는 3일간 유효)
3. 매장이 서류 업로드·서명 완료
4. **계약서 선택/첨부** → 서류 5개 완성 확인
5. `NHN-LOGIN.bat` 으로 NHN 로그인(안 돼 있으면) → **NHN 등록 신청** 클릭
6. 심사 상태는 자동 갱신되어 대시보드에 표시
   - **등록완료**: NHN 승인 → 자동 표시 (승인 시 매장 연락처로 안내 문자 자동 발송)
   - **승인 미확인**: 1영업일이 지나도 승인이 확인되지 않으면 표시 → NHN 콘솔·이메일에서 **직접 확인** (반려됐으면 서류 교체 후 재신청)

---

## 자주 묻는 것

- **Node.js 설치해야 하나요?**
  아니요. 이 번들에 실행기가 포함돼 있어 **압축만 풀면 됩니다.**

- **웹 주소로 접속하면 안 되나요?**
  이 툴은 각자 PC에서 본인 NHN 계정으로 제출해야 해서, 내 PC에서 켜고 `localhost:3000` 으로 씁니다. (공용 서버 아님)

- **`START.bat` 을 눌렀는데 창이 바로 꺼지거나 ".env 없음" 이 떠요.**
  `.env` 파일이 폴더 안에 없는 거예요. 관리자에게 받아 `START.bat` 과 같은 위치에 넣고 다시 실행하세요.

- **"address already in use :::3000" 오류가 떠요.**
  이미 프로그램이 켜져 있는 거예요. 열려 있는 다른 검은 창을 닫고 다시 `START.bat` 을 실행하세요.

- **검은 창을 닫았어요.**
  프로그램이 꺼진 거예요. 다시 `START.bat` 을 더블클릭하면 됩니다.

- **다른 사람 매장도 보이나요?**
  아니요. 각 PC는 본인이 만든 매장만 관리합니다. (데이터가 PC별로 따로 저장돼요)
