# 발신번호 등록 관리자 — 팀원 설치 가이드

각자 **본인 PC에 한 번만 설치**하면, 이후에는 매일 실행 파일만 더블클릭해서 씁니다.
(웹 주소로 접속하는 게 아니라, 내 PC에서 프로그램을 켜고 브라우저로 여는 방식이에요.)

---

## 처음 한 번만 (설치)

### 1. Node.js 설치
- https://nodejs.org 에서 **LTS 버전**을 받아 설치하세요. (다음 → 다음만 누르면 됩니다)
- 설치되면 재부팅까지는 필요 없어요.

### 2. 프로그램 폴더 받기
- 관리자(김시온)에게 프로그램 폴더(또는 GitHub 다운로드 zip)를 받아 **원하는 위치에 압축을 풀어** 두세요.
  - 예: `바탕화면\store-doc-portal`

### 3. 설정 파일(.env) 넣기
- 관리자에게 **`.env` 파일**을 받아 **프로그램 폴더 안에** 그대로 넣으세요.
  (구글 로그인·NHN·문자 발송에 필요한 공용 설정값이 들어 있습니다. 개인이 만들 필요 없어요.)
- ⚠️ `.env` 파일은 비밀값이 있으니 **외부에 공유하지 마세요.**

### 4. 설치 실행
- 폴더 안의 **`설치.bat`** 을 더블클릭하세요.
- 패키지 설치 + 자동화용 브라우저 설치가 진행됩니다. (몇 분 걸릴 수 있어요)
- "Setup done" 이 뜨면 완료입니다.

---

## 매일 사용하기

### A. 프로그램 켜기
- **`실행.bat`** 더블클릭 → 검은 창이 뜨고 서버가 켜집니다. **이 창은 켜 두세요.** (닫으면 꺼져요)
- 브라우저에서 **http://localhost:3000** 접속 → **본인 회사 구글 계정**으로 로그인 → 사용 시작

### B. NHN 로그인 (발신번호 등록 신청할 때만)
- 발신번호 **등록 신청(제출)** 을 하려면 NHN 로그인이 필요합니다.
- **`로그인.bat`** 더블클릭 → 브라우저가 열리면 **본인 NHN 계정**으로 로그인 → 안내대로 Enter
- 한 번 로그인하면 세션이 유지되고, 대시보드 상단 배너에 **"NHN 로그인됨"** 으로 표시됩니다.
  - 배너가 **"NHN 로그인되어 있지 않습니다"** 로 바뀌면(세션 만료) → `로그인.bat` 을 다시 실행하세요.

---

## 사용 흐름 요약

1. `실행.bat` → localhost:3000 접속 → 구글 로그인
2. 매장 검색 → **링크 발급** → 매장에 전달 (링크는 3일간 유효)
3. 매장이 서류 업로드·서명 완료
4. **계약서 선택/첨부** → 서류 5개 완성 확인
5. `로그인.bat` 으로 NHN 로그인(안 돼 있으면) → **NHN 등록 신청** 클릭
6. 심사 상태(심사중 → 등록완료/거부)는 자동으로 갱신되어 대시보드에 표시
7. 승인되면 매장 연락처로 안내 문자 자동 발송

---

## 자주 묻는 것

- **웹 주소로 접속하면 안 되나요?**
  이 툴은 각자 PC에서 본인 NHN 계정으로 제출해야 해서, 내 PC에서 켜고 `localhost:3000` 으로 씁니다. (공용 서버 아님)

- **검은 창(실행.bat)을 닫았어요.**
  프로그램이 꺼진 거예요. 다시 `실행.bat` 을 더블클릭하면 됩니다.

- **`설치.bat` 실행 중 오류가 나요.**
  Node.js가 설치됐는지 확인하고, 그래도 안 되면 관리자에게 창에 보이는 오류 메시지를 그대로 캡처해 전달하세요.

- **다른 사람 매장도 보이나요?**
  아니요. 각 PC는 본인이 만든 매장만 관리합니다. (데이터가 PC별로 따로 저장돼요)
